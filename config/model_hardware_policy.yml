# Perpetua-Tools/config/model_hardware_policy.yml
# SINGLE SOURCE OF TRUTH — all enforcement reads this file
#
# Safety context:
# - lmstudio-mac must never advertise or run Windows-only 27B/26B GGUF models.
# - lmstudio-win must never advertise or run Mac-only MLX / Apple Silicon models.
# - shared is intentionally empty until a model is verified on both physical hosts.

windows_only:
  - gemma-4-26b-a4b-it
  - gemma-4-26B-A4B-it-Q4_K_M
  - qwen3.5-27b-claude-4.6-opus-reasoning-distilled-v2
  - Qwen3.5-27B-Claude-4.6-Opus-Reasoning-Distilled-v2

mac_only:
  - gemma-4-e4b-it
  - qwen3.5-9b-mlx
  - qwen3.5-9b-mlx-4bit
  - Qwen3.5-9B-MLX-4bit

# TODO(shared-models): Populate this section once both Mac and Windows machines
# are online simultaneously and `discover.py --status` has been run on both.
# Steps documented in: docs/tripartite-plan/2026-04-26-hardware-model-routing-004-PART2-PLAN.md § Phase 5
# Out of scope until both machines are verified online.
# shared: