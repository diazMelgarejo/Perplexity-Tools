# Perpetua-Tools/config/model_hardware_policy.yml
# SINGLE SOURCE OF TRUTH — all enforcement reads this file
#
# Safety context:
# - lmstudio-mac must never advertise or run Windows-only 27B/26B GGUF models.
# - lmstudio-win must never advertise or run Mac-only MLX / Apple Silicon models.
# - shared is intentionally empty until a model is verified on both physical hosts.

windows_only:
  - gemma-4-26b-a4b-it
  - gemma-4-26B-A4B-it-Q4_K_M
  - qwen3.5-27b-claude-4.6-opus-reasoning-distilled-v2
  - Qwen3.5-27B-Claude-4.6-Opus-Reasoning-Distilled-v2

mac_only:
  - gemma-4-e4b-it
  - qwen3.5-9b-mlx
  - qwen3.5-9b-mlx-4bit
  - Qwen3.5-9B-MLX-4bit

shared:
  # INTENTIONALLY EMPTY — do not populate without verification
  # Add only confirmed cross-platform models here